[pytest]
markers =
    llm_live: exercises the real OpenAI API (requires OPENAI_API_KEY and RUN_LLM_LIVE=1).
    llm_regression: offline LLM pipeline regression (mocked completions).
    advisor_live: calls a running flask_sample meeting_advisor (MEETING_ADVISOR_URL + RUN_MEETING_ADVISOR_LIVE=1).

testpaths = tests
